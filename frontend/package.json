{
  "name": "my-habitica-pets",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",

    "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc && cp index-prod.html dist/index.html && cp style.css dist/style.css && cp -R assets dist/assets"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
